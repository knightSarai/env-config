# Small helper scripts kept in this repo.
- name: Install tmux-sessionizer
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/tmux-sessionizer"
    dest: "{{ local_bin }}/tmux-sessionizer"
    mode: "0755"
